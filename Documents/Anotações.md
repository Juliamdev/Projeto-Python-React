>cd backend
# Criando o ambiente virtual
>python -m venv venv

# Ativando no Windows (Prompt de Comando):
>venv\Scripts\activate

>pip install fastapi uvicorn
>pip freeze > requirements.txt

frontend(cria a pasta)
>npx create-expo-app@latest frontend

Escolha a opção Select an Expo SDK version >> For learning with Expo Go (SDK 54)